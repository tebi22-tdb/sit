# Opcional: desarrollo local con front en :4200 — activar con --spring.profiles.active=dev
# (misma BD que application.properties; solo ajusta cookies de sesión para localhost)
server.servlet.session.cookie.domain=localhost
server.servlet.session.cookie.path=/
server.servlet.session.cookie.http-only=true
